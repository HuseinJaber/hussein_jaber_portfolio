# Dependencies
node_modules/
vendor/

# Environment
.env
.env.*
!.env.example
!**/.env.example
!**/.env.production.example

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Logs
*.log
npm-debug.log*
yarn-error.log*

# PHP / Laravel
.phpunit.result.cache
.phpunit.cache
auth.json
Homestead.json
Homestead.yaml

# Build artifacts (rebuilt on deploy)
backend/public/build/
backend/public/hot
frontend/.next/
frontend/out/
frontend/build/
frontend/dist/

# Next.js archive (keep locally; optional source folder may be committed)
frontend-nextjs-backup.zip

# Database exports (import via phpMyAdmin — never commit)
portfolio_export.sql
*.sql.gz

# Local deploy packages (upload to Hostinger manually)
backend-build-upload.zip

# Laravel storage (keep structure via nested .gitignore files)
backend/storage/*.key
